# Docs

This folder keeps non-code project documentation organized into two groups:

- `planning/` for product vision, pathways, and role-flow documents
- `reports/` for progress reports and daily updates

This keeps the repository root cleaner while making project documents easier to find.
